# 腾讯云 COS 配置
# 复制本文件为 .env 并填入真实值
# 本文件可被提交到 Git，仅作为模板

# 腾讯云 COS 配置
COS_SECRET_ID=your-secret-id
COS_SECRET_KEY=your-secret-key
COS_BUCKET=your-bucket-name
COS_REGION=your-region
COS_DOMAIN=your-cos-domain
COS_PUBLIC_PREFIX=public/
COS_PRIVATE_PREFIX=private/

# 数据库配置
JDBC_USERNAME=your-db-username
JDBC_PASSWORD=your-db-password

# 说明：
# 1. 复制本文件为 .env 后，将其中的占位符替换为真实配置。
# 2. .env 文件已被 Git 忽略，不会提交到仓库。
# 3. 本地开发时，Spring 默认从当前工作目录下的 .env 文件读取这些值。
#    在 IDEA 中运行 Tomcat 时，请将 Tomcat 运行配置的 "Working directory" 设置为项目根目录，
#    或在该运行配置的 VM options 中添加：
#    -DAPP_ENV_PATH=E:/backend_Develop/0-JavaWeb/PersonalSite/PersonalSite/.env
# 4. 使用 Tomcat 部署时，若 Tomcat 启动目录不是项目根目录，可：
#    - 将 .env 文件放到 Tomcat 启动目录（通常为 bin 目录），或
#    - 在 setenv.bat/setenv.sh 中设置 APP_ENV_PATH 指向 .env 文件，或
#    - 直接设置系统环境变量（如 COS_SECRET_ID、JDBC_USERNAME 等）。
